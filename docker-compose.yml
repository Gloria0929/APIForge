services:
  apiforge:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - apiforge-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_TYPE=sqlite
      - DB_SQLITE_PATH=/app/data/apiforge.db
      - PORT=3000
    restart: unless-stopped

volumes:
  apiforge-data:
